//! `HistoryRepo` implementation backed by `sqlx` + SQLite. See
//! implementation plan §8a and §16.

// TODO M6: implement `HistoryRepo` against an `sqlx::SqlitePool`.
// TODO M6: add round-trip tests against a temp/in-memory SQLite with
// TODO M6:   migrations applied.
